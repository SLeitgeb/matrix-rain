# Install

Clone the respository:
```sh
git clone git@gitlab.com:SLeitgeb/matrix-rain.git
```

And install `matrix-rain` executable to your `go/bin` path:
```sh
go install
```

Or run from inside the repository:
```sh
go run matrix-rain
```
